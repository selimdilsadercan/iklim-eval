{
  "name": "iklim-eval-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "İklim Eval Monorepo - Evaluator (Python), Backend (Encore), Frontend",
  "scripts": {
    "evaluator:setup": "cd evaluator && setup.bat",
    "evaluator:api": "cd evaluator && npm run api",
    "eval": "cd evaluator && npm run eval --"
  }
}
